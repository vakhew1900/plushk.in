---
name: scaffold
description: Create a new React component, or a new service/repository (interface + implementation + test), following this project's layered architecture and coding conventions. Use when asked to add/create a component, add/create a service, add/create a repository, or scaffold new code under src/components, src/services, or src/repository.
---

# Scaffold

Full instructions live in [../../../.claude/skills/scaffold/SKILL.md](../../../.claude/skills/scaffold/SKILL.md) — read that file (and its `reference/component.md` / `reference/service.md`) for the actual rules. Not duplicated here so the two copies can't drift out of sync; this file exists only so skill discovery under `.agents/skills/` finds `scaffold` at all.
